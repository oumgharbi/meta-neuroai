# this is used for the pre-commit hooks
# and when running from the root folder (eg: in the CI)

[tool.uv]
# Ignore releases younger than this to let compromised uploads get flagged.
exclude-newer = "3 days"

# Packages we publish — exempt from the window above.
[tool.uv.exclude-newer-package]
exca = "2099-01-01"
submitit = "2099-01-01"

[tool.ruff]
line-length = 90
exclude = [
    "__editable__",
    "docs/pythonplusplus",
    "*.ipynb",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "W",   # pycodestyle warnings
    "A",   # flake8-builtins (parity: redefined-builtin)
    "B006",  # mutable default argument (parity: dangerous-default-value)
    "PIE790",  # unnecessary-placeholder (rough parity: unnecessary-pass)
]
ignore = [
    "E501",  # line too long (formatter handles this)
    "E712",  # == True comparisons (intentional in pandas boolean filtering)
    "E722",  # bare except (intentional in defensive data-loading code)
    "E731",  # lambda assignment (common pattern in tests)
    "E741",  # ambiguous variable name (l, O — intentional in math code)
]

[tool.ruff.lint.isort]
known-first-party = ["neuralset", "neuraltrain", "neuralfetch", "neuralbench"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "F401",  # re-exports
    "E402",  # late imports after version check
]
"test_*.py" = ["F811"]   # pytest fixture redefinitions
"**/tests/**" = ["F811"]  # pytest fixture redefinitions
"docs*/**/*.py" = [
    "A001",  # copyright is a standard Sphinx config variable
    "E402",  # sphinx-gallery import structure
    "F811",  # sphinx setup function redefinitions
]

[tool.mypy]
plugins = ["pydantic.mypy"]

[tool.codespell]
# Use `codespell[toml]` for local runs — plain `codespell` ignores this config.
# Ref: https://github.com/codespell-project/codespell#using-a-config-file
skip = '.git,.git-meta,.gitignore,.gitattributes,*.css,*/.venv/*,*/.tox/*,*_explore_data.html'
check-hidden = true
# Ignore URLs: URL paths may contain sequences that look like typos
ignore-regex = 'https?://\S+'
# Skip `bibtex: ... = """..."""` Study attributes (verbatim citations we
# can't edit), and any block between `codespell:ignore-begin/-end` markers
# (use those in RST/MD where inline `# codespell:ignore` doesn't render
# cleanly). Single-line false positives use `# codespell:ignore <word>`.
ignore-multiline-regex = 'bibtex[^\n]*= """[\s\S]*?"""|codespell:ignore-begin[\s\S]*?codespell:ignore-end'
# fro: PyTorch Frobenius norm parameter, e.g. x.norm(p="fro")
# toi, rouge: French words ("you", "red") used in accent-insensitive matching tests
# fo: test timeline identifier string
# lond: London site code in Nieuwland2018 filenames
# notin: Python AST class ast.NotIn
ignore-words-list = 'fro,toi,rouge,fo,lond,notin'
